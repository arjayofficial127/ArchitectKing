/**
 * Feature flags — flip to enable/disable UI features without removing code.
 */

export const FEATURES = {
  /** Floating music player rendered globally in the root layout. */
  musicPlayer: false,
} as const;
